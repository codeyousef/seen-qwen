manifest-version = 1

[project]
name = "seen_qwen"
version = "0.15.0-dev"
language = "en"
visibility = "private"
edition = "2025"
modules = [
    "src/model/config.seen",
]

[build]
targets = ["native"]
optimize = "speed"
profile = "release"

[dependencies]
