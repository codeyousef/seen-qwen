manifest-version = 1

[project]
name = "seen_qwen"
version = "0.15.0-dev"
language = "en"
visibility = "private"
edition = "2025"
modules = [
    "src/model/config.seen",
    "src/model/tensor_catalog.seen",
    "src/model/contract_geometry.seen",
    "src/model/cpu_reference.seen",
    "src/model/cpu_attention.seen",
    "src/tokenizer/tokenizer.seen",
]

[build]
targets = ["native"]
optimize = "speed"
profile = "release"

[dependencies]
